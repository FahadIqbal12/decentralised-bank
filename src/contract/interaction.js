import { ethers } from 'ethers';
export const provider = new ethers.providers.JsonRpcProvider(``); // Provider url here
export const contractAddress = '0x000000000000000000000000';
export const abi = [
	//contract abi goes here
]
